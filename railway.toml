[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn clipai.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
healthcheckPath = "/api/auth/health/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
